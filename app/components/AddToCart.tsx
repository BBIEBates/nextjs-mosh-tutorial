"use client";
import React from "react";

const AddToCart = () => {
  return <button className="btn btn-primary" onClick={() => console.log("clicked!")}>add to cart</button>;
};

export default AddToCart;
